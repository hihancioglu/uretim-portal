# TeknikResimOlcum SQL yapılandırması
# Bu dosyayı Data\Database.config olarak kopyalayın.
# Mode=CSV kalırsa program mevcut CSV altyapısıyla çalışır.
# Mode=SQL yapılırsa başlangıçta SQL şema kontrolü çalışır.

Mode=CSV
ConnectionString=Server=.\SQLEXPRESS;Database=TeknikResimOlcum;Trusted_Connection=True;TrustServerCertificate=True;
